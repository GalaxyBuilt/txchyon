---
// src/pages/index.astro - FINAL REDESIGN: Ecosystem + Education
import { getCollection } from 'astro:content';
import Base from "@/layouts/Base.astro";
import Posts from "@/layouts/components/Posts.astro";
import Pagination from "@/layouts/components/Pagination.astro";
import { CATEGORY_HIERARCHY } from '@/lib/categories';
import GenesisPreview from "../partials/GenesisPreview.astro";

// Helper function for automatic readTime calculation
function calculateReadTime(content, wordsPerMinute = 130) {
  if (!content) return 10;
  
  const plainText = content
    .replace(/---[\s\S]*?---/, '')
    .replace(/```[\s\S]*?```/g, '')
    .replace(/<[^>]*>/g, '')
    .replace(/#{1,6}\s*/g, '')
    .replace(/[\[\]\(\)]/g, ' ')
    .replace(/\s+/g, ' ')
    .trim();
  
  const wordCount = plainText.split(/\s+/).length;
  const minutes = Math.ceil(wordCount / wordsPerMinute);
  
  return Math.max(1, Math.min(minutes, 60));
}

function getPostReadTime(post) {
  if (post.data.readTime) return post.data.readTime;
  if (post.body) return calculateReadTime(post.body);
  
  const estimates = {
    'getting-started': 8,
    'security-privacy': 10,
    'trading-investing': 12,
    'defi-yield': 15,
    'airdrop-farming': 12,
    'infrastructure-tech': 14,
    'tools-automation': 13,
    'research-analysis': 15,
    'regulatory-tax': 12,
    'portfolio-management': 12,
    'prediction-markets': 14
  };
  
  return estimates[post.data.category] || 10;
}

const allPostsRaw = await getCollection('posts', ({ data }) => !data.draft);
const allPosts = allPostsRaw.map(post => ({
  ...post,
  data: {
    ...post.data,
    readTime: getPostReadTime(post)
  }
}));

const sortedPosts = allPosts.sort(
  (a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime()
);

const featuredPost = sortedPosts[0];
const latestPosts = sortedPosts.slice(1, 8);

const categoryPosts = {};
const categoryPostCounts = {};

Object.keys(CATEGORY_HIERARCHY).forEach(category => {
  const allPostsInCategory = sortedPosts.filter(p => p.data.category === category);
  categoryPostCounts[category] = allPostsInCategory.length;
  categoryPosts[category] = allPostsInCategory
    .filter(p => p.slug !== featuredPost?.slug)
    .slice(0, 3);
});

const gettingStartedPosts = sortedPosts
  .filter(p => p.data.category === 'getting-started' && p.slug !== featuredPost?.slug);

let quickWins;
if (gettingStartedPosts.length >= 4) {
  quickWins = gettingStartedPosts.slice(0, 4);
} else {
  const otherPosts = sortedPosts
    .filter(p => p.slug !== featuredPost?.slug)
    .slice(0, 4 - gettingStartedPosts.length);
  quickWins = [...gettingStartedPosts, ...otherPosts].slice(0, 4);
}

if (!quickWins || quickWins.length === 0) {
  quickWins = sortedPosts.slice(0, 4);
}

const realisticStats = {
  guidesPublished: allPosts.length.toString(),
  activeReaders: "1,200+",
  protocolsCovered: "45+",
  monthsActive: "1"
};

const matrixCategories = [...new Set(allPostsRaw.map(p => p.data.category))].slice(0, 6);
---

<Base title="Txchyon OS // Open-Source Infrastructure // Private Intelligence" 
      description="Professional-grade open-source DeFi research and systematic alpha. Private intelligence for elite capital, delivered with institutional integrity.">

  <!-- ₿ GENESIS TERMINAL PREVIEW (TOP LEVEL) -->
  <GenesisPreview />

  <!-- 🕹️ EXECUTION HUB (FULL SCALE V3 MATRIX) -->
  <section class="py-32 dark:bg-black bg-white selection:bg-cyan-500 selection:text-black font-mono relative overflow-hidden border-y border-gray-100 dark:border-white/5">
    <!-- Tier 3: Dot Grid Background + Solana Glows -->
    <div class="absolute inset-0 pointer-events-none z-0" style="background-image: radial-gradient(circle, #9333ea 1px, transparent 1px); background-size: 32px 32px; opacity: 0.06;"></div>
    <div class="absolute top-0 left-0 w-full h-full pointer-events-none opacity-10 dark:opacity-40 z-0">
        <div class="absolute top-[-10%] left-[-10%] w-[40%] h-[40%] bg-purple-600/30 blur-[120px] rounded-full"></div>
        <div class="absolute bottom-[-10%] right-[-10%] w-[40%] h-[40%] bg-cyan-600/30 blur-[120px] rounded-full"></div>
    </div>

    <div class="container mx-auto px-4 relative z-10">
      <div class="flex flex-col md:flex-row justify-between items-center md:items-end mb-20 gap-8">
        <div class="animate-fade-in-up" style="animation-delay: 0.1s;">
          <!-- Tier 2: Gradient Text on Section Header -->
          <h2 class="text-5xl md:text-7xl font-black mb-6 tracking-tighter uppercase italic leading-[0.85] dark:text-white text-gray-900">
            The <span class="text-transparent bg-clip-text bg-gradient-to-r from-purple-500 via-cyan-500 to-green-400">Execution</span><br/>Hub<span class="blinking-cursor">_</span>
          </h2>
          <div class="h-1.5 w-32 bg-gradient-to-r from-purple-500 to-cyan-500 mb-4"></div>
          <p class="text-[10px] font-bold text-gray-500 dark:text-gray-400 tracking-[0.4em] uppercase">Open-Source Access // Multi-Protocol Interface</p>
        </div>
        
        <!-- Tier 1: Counter Animation on Stats -->
        <div class="flex gap-8 text-[10px] font-black items-center text-gray-400 uppercase tracking-widest border border-gray-100 dark:border-white/10 p-6 rounded-2xl bg-gray-50/50 dark:bg-white/5 backdrop-blur-md animate-fade-in-up" style="animation-delay: 0.2s;">
          <div class="flex flex-col gap-1">
             <span class="opacity-50">Memory</span>
             <span class="text-purple-500" data-counter-target="128">0</span><span class="text-purple-500">TB_USED</span>
          </div>
          <div class="w-px h-8 bg-gray-200 dark:bg-white/10"></div>
          <div class="flex flex-col gap-1">
             <span class="opacity-50">Network</span>
             <span class="text-cyan-500" data-counter-target="10">0</span><span class="text-cyan-500">GBIT/S</span>
          </div>
          <div class="w-px h-8 bg-gray-200 dark:bg-white/10"></div>
          <div class="flex flex-col gap-1">
             <span class="opacity-50">Security</span>
             <span class="text-green-400">STABLE</span>
          </div>
        </div>
      </div>

      <!-- Tier 2: Glassmorphism Cards with Hover Lift -->
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 max-w-7xl mx-auto">
        <!-- Mission 01: Execute (Labs) -->
        <div class="group relative overflow-hidden bg-gray-50/50 dark:bg-[#080808] border border-gray-100 dark:border-white/10 p-10 hover:bg-gray-100 dark:hover:border-cyan-500/50 transition-all cursor-pointer rounded-[2rem] shadow-xl dark:shadow-cyan-500/5 backdrop-blur-md hover:-translate-y-2 animate-fade-in-up" style="animation-delay: 0.3s;">
          <!-- Tier 1: Animated Gradient Border on EXECUTE card -->
          <div class="absolute inset-0 rounded-[2rem] p-[2px] opacity-0 group-hover:opacity-100 transition-opacity duration-500">
            <div class="absolute inset-0 rounded-[2rem] bg-gradient-to-r from-cyan-500 via-purple-500 to-pink-500 animate-spin-slow"></div>
          </div>
          <div class="relative z-10">
            <div class="absolute top-0 right-0 p-6 opacity-20 text-[10px] dark:text-white text-gray-900 font-black">PROTOCOL_EXE_001</div>
            <div class="flex flex-col h-full justify-between min-h-[320px]">
              <div>
                <div class="text-cyan-500 text-[10px] font-black mb-4 tracking-[0.3em]">LAYER_01 // INFRASTRUCTURE</div>
                <h3 class="text-4xl font-black mb-6 italic text-gray-900 dark:text-transparent dark:bg-clip-text dark:bg-gradient-to-r dark:from-cyan-400 dark:to-purple-400 group-hover:text-transparent group-hover:bg-clip-text group-hover:bg-gradient-to-r group-hover:from-cyan-500 group-hover:to-purple-500 transition-all">EXECUTE</h3>
                <p class="text-sm dark:text-gray-300 text-gray-600 leading-relaxed italic">Institutional-grade research and execution terminals for Solomon and Bitcoin natives.</p>
              </div>
              <div class="flex flex-col gap-4">
                <a href="/labs" class="inline-block w-fit px-8 py-4 bg-gray-900 dark:bg-white dark:text-black text-white font-black text-xs uppercase tracking-widest hover:bg-gradient-to-r from-purple-500 to-cyan-500 dark:hover:text-white transition-colors rounded-xl shadow-lg">Launch Terminals</a>
              </div>
            </div>
          </div>
        </div>

        <!-- Mission 02: Grow (Services) -->
        <div class="group relative overflow-hidden bg-gray-50/50 dark:bg-[#080808] border border-gray-100 dark:border-white/10 p-10 hover:bg-gray-100 dark:hover:border-purple-500/50 transition-all cursor-pointer rounded-[2rem] shadow-xl dark:shadow-purple-500/5 backdrop-blur-md hover:-translate-y-2 animate-fade-in-up" style="animation-delay: 0.4s;">
          <div class="absolute top-0 right-0 p-6 opacity-20 text-[10px] dark:text-white text-gray-900 font-black">PROTOCOL_SRV_088</div>
          <div class="flex flex-col h-full justify-between min-h-[320px]">
             <div>
                <div class="text-purple-500 text-[10px] font-black mb-4 tracking-[0.3em]">LAYER_02 // ACTIVATION</div>
                  <h3 class="text-4xl font-black mb-6 italic text-gray-900 dark:text-transparent dark:bg-clip-text dark:bg-gradient-to-r dark:from-purple-400 dark:to-pink-400 group-hover:text-purple-500 transition-colors">GROW</h3>
                <p class="text-sm dark:text-gray-400 text-gray-600 mb-8 leading-relaxed italic">Professional marketing, narrative audits, and viral activation for elite protocols.</p>
             </div>
             <a href="/services" class="text-[11px] font-black text-purple-500 hover:tracking-[0.2em] transition-all uppercase underline decoration-2 underline-offset-8">Apply_For_Services →</a>
          </div>
        </div>

        <!-- Mission 03: Access (Shop) -->
        <div class="group relative overflow-hidden bg-gray-50/50 dark:bg-[#080808] border border-gray-100 dark:border-white/10 p-10 hover:bg-gray-100 dark:hover:border-orange-500/50 transition-all cursor-pointer rounded-[2rem] shadow-xl dark:shadow-orange-500/5 backdrop-blur-md hover:-translate-y-2 animate-fade-in-up" style="animation-delay: 0.5s;">
          <div class="absolute top-0 right-0 p-6 opacity-20 text-[10px] dark:text-white text-gray-900 font-black">PROTOCOL_PASS_003</div>
          <div class="flex flex-col h-full justify-between min-h-[320px]">
             <div>
                <div class="text-orange-500 text-[10px] font-black mb-4 tracking-[0.3em]">LAYER_03 // MEMBERSHIP</div>
                  <h3 class="text-4xl font-black mb-6 italic text-gray-900 dark:text-transparent dark:bg-clip-text dark:bg-gradient-to-r dark:from-orange-400 dark:to-amber-400 group-hover:text-orange-500 transition-colors">ACCESS</h3>
                <p class="text-sm dark:text-gray-400 text-gray-600 mb-8 leading-relaxed italic">Institutional monthly passes and systematic playbook repository for serious capital.</p>
             </div>
             <a href="/shop" class="text-[11px] font-black text-orange-500 hover:tracking-[0.2em] transition-all uppercase underline decoration-2 underline-offset-8">Enter_Shop →</a>
          </div>
        </div>

        <!-- Mission 04: Alpha (Insider) -->
        <div class="group relative overflow-hidden bg-gray-50/50 dark:bg-[#080808] border border-gray-100 dark:border-white/10 p-10 hover:bg-gray-100 dark:hover:border-yellow-500/50 transition-all cursor-pointer rounded-[2rem] shadow-xl dark:shadow-yellow-500/5 backdrop-blur-md hover:-translate-y-2 animate-fade-in-up" style="animation-delay: 0.6s;">
          <div class="absolute top-0 right-0 p-6 opacity-20 text-[10px] dark:text-white text-gray-900 font-black">PROTOCOL_ALPH_99</div>
          <div class="flex flex-col h-full justify-between min-h-[320px]">
            <div>
              <div class="text-yellow-500 text-[10px] font-black mb-4 tracking-[0.3em]">LAYER_04 // INTELLIGENCE</div>
              <h3 class="text-4xl font-black mb-6 italic text-gray-900 dark:text-transparent dark:bg-clip-text dark:bg-gradient-to-r dark:from-yellow-400 dark:to-orange-400 group-hover:text-yellow-500 transition-colors">ALPHA</h3>
              <p class="text-sm dark:text-gray-300 text-gray-600 leading-relaxed italic">Exclusive early DEX arbitrage, whitelists, and high-frequency discovery systems.</p>
            </div>
            <a href="/categories/alpha-insider" class="text-[11px] font-black text-yellow-500 hover:tracking-[0.2em] transition-all uppercase underline decoration-2 underline-offset-8">Explore_Alpha →</a>
          </div>
        </div>

        <!-- Mission 05: Tools (Automation) -->
        <div class="group relative overflow-hidden bg-gray-50/50 dark:bg-[#080808] border border-gray-100 dark:border-white/10 p-10 hover:bg-gray-100 dark:hover:border-green-500/50 transition-all cursor-pointer rounded-[2rem] shadow-xl dark:shadow-green-500/5 backdrop-blur-md hover:-translate-y-2 animate-fade-in-up" style="animation-delay: 0.7s;">
          <div class="absolute top-0 right-0 p-6 opacity-20 text-[10px] dark:text-white text-gray-900 font-black">PROTOCOL_TOOL_404</div>
          <div class="flex flex-col h-full justify-between min-h-[320px]">
            <div>
              <div class="text-green-500 text-[10px] font-black mb-4 tracking-[0.3em]">LAYER_05 // AUTOMATION</div>
              <h3 class="text-4xl font-black mb-6 italic text-gray-900 dark:text-transparent dark:bg-clip-text dark:bg-gradient-to-r dark:from-green-400 dark:to-cyan-400 group-hover:text-green-500 transition-colors">TOOLS</h3>
              <p class="text-sm dark:text-gray-400 text-gray-600 leading-relaxed italic">Proprietary trading bots, alert systems, and high-speed automation scripts.</p>
            </div>
            <a href="/categories/tools-automation" class="text-[11px] font-black text-green-500 hover:tracking-[0.2em] transition-all uppercase underline decoration-2 underline-offset-8">Deploy_Tools →</a>
          </div>
        </div>

        <!-- Mission 06: Master (Academy) -->
        <div class="group relative overflow-hidden bg-gray-50/50 dark:bg-[#080808] border border-gray-100 dark:border-white/10 p-10 hover:bg-gray-100 dark:hover:border-blue-500/50 transition-all cursor-pointer rounded-[2rem] shadow-xl dark:shadow-blue-500/5 backdrop-blur-md hover:-translate-y-2 animate-fade-in-up" style="animation-delay: 0.8s;">
          <div class="absolute top-0 right-0 p-6 opacity-20 text-[10px] dark:text-white text-gray-900 font-black">PROTOCOL_EDU_011</div>
          <div class="flex flex-col h-full justify-between min-h-[320px]">
            <div>
              <div class="text-blue-500 text-[10px] font-black mb-4 tracking-[0.3em]">LAYER_06 // KNOWLEDGE</div>
              <h3 class="text-4xl font-black mb-6 italic text-gray-900 dark:text-transparent dark:bg-clip-text dark:bg-gradient-to-r dark:from-blue-400 dark:to-purple-400 group-hover:text-blue-500 transition-colors">MASTER</h3>
              <p class="text-sm dark:text-gray-400 text-gray-600 leading-relaxed italic">The complete Txchyon repository. 11 learning paths covering every DeFi niche.</p>
            </div>
            <a href="#start-learning" class="text-[11px] font-black text-blue-500 hover:tracking-[0.2em] transition-all uppercase underline decoration-2 underline-offset-8">Begin_Mastery →</a>
          </div>
        </div>
      </div>
    </div>
    <style>
      @keyframes fadeInUp {
        from {
          opacity: 0;
          transform: translateY(20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }
      .animate-fade-in-up {
        animation: fadeInUp 0.6s ease-out forwards;
        opacity: 0; /* Start hidden */
      }

      @keyframes spin-slow {
        from { transform: rotate(0deg); }
        to { transform: rotate(360deg); }
      }
      .animate-spin-slow {
        animation: spin-slow 8s linear infinite;
      }

      @keyframes blink {
        50% { opacity: 0; }
      }
      .blinking-cursor {
        animation: blink 1s step-end infinite;
      }
    </style>
    <script>
      document.addEventListener('DOMContentLoaded', () => {
        const observerOptions = {
          root: null,
          rootMargin: '0px',
          threshold: 0.1
        };

        const observer = new IntersectionObserver((entries, observer) => {
          entries.forEach(entry => {
            if (entry.isIntersecting) {
              // Counter animation
              const counters = entry.target.querySelectorAll('[data-counter-target]');
              counters.forEach(counter => {
                const target = parseInt(counter.dataset.counterTarget);
                let current = 0;
                const increment = target / 100; // Adjust for smoother animation
                const updateCounter = () => {
                  if (current < target) {
                    current += increment;
                    counter.textContent = Math.ceil(current);
                    requestAnimationFrame(updateCounter);
                  } else {
                    counter.textContent = target;
                  }
                };
                updateCounter();
              });

              // Fade-in-up animation
              entry.target.querySelectorAll('.animate-fade-in-up').forEach(el => {
                el.style.opacity = '0'; // Ensure it starts hidden
                el.style.animationPlayState = 'running';
              });
              observer.unobserve(entry.target);
            }
          });
        }, observerOptions);

        const section = document.querySelector('section.py-32'); // Target the Execution Hub section
        if (section) {
          observer.observe(section);
        }
      });
    </script>
  </section>

  <!-- 🧠 INTEL DATABASE (PROTOCOL_DOCS) -->
  <section class="py-24 dark:bg-black bg-white border-b border-gray-100 dark:border-white/5 relative overflow-hidden">
    <!-- Solana-inspired background glows -->
    <div class="absolute top-1/2 left-1/4 w-[500px] h-[500px] bg-cyan-500/5 rounded-full blur-[120px] -translate-y-1/2 animate-pulse-slow"></div>
    <div class="absolute top-1/2 right-1/4 w-[500px] h-[500px] bg-purple-500/5 rounded-full blur-[120px] -translate-y-1/2 animate-pulse-slower"></div>

    <div class="container mx-auto px-4 relative z-10">
      <div class="dark:bg-black/40 bg-white/40 border dark:border-white/10 border-gray-200 p-12 md:p-16 relative overflow-hidden rounded-[3rem] shadow-2xl backdrop-blur-md">
        <!-- Accent Gradient Bar -->
        <div class="absolute top-0 left-0 right-0 h-1.5 bg-gradient-to-r from-cyan-500 via-purple-500 to-pink-500 opacity-80"></div>
        
        <div class="absolute top-0 right-0 p-10 opacity-10 text-[10px] dark:text-white text-black font-black tracking-[1em] uppercase vertical-text">INTEL_REPOSITORY_V3.1</div>
         <div class="grid grid-cols-1 lg:grid-cols-2 gap-16">
           <div class="flex flex-col justify-center">
              <div class="inline-flex items-center gap-3 mb-6 px-4 py-2 bg-gradient-to-r from-cyan-500/10 to-purple-500/10 border border-cyan-500/20 rounded-full w-fit">
                 <span class="text-transparent bg-clip-text bg-gradient-to-r from-cyan-500 to-purple-500 text-[10px] font-black tracking-widest uppercase italic">Open-Source Intelligence Layer</span>
              </div>
              <h3 class="text-5xl md:text-7xl font-black mb-10 italic tracking-tighter dark:text-white text-gray-900 leading-[0.8] uppercase">
                Intel<br/>
                <span class="text-transparent bg-clip-text bg-gradient-to-r from-cyan-500 via-purple-500 to-pink-500">Database_</span>
              </h3>
              <p class="text-base dark:text-gray-400 text-gray-600 mb-10 leading-relaxed max-w-md italic font-medium">
                Access the open-source DeFi research repository. 11 Pillars of systematic intelligence. 0 Gates. 100% Contributor-Driven Information.
              </p>
              
              <!-- Stats Row -->
              <div class="grid grid-cols-2 gap-8 mb-10 border-t border-gray-100 dark:border-white/5 pt-8">
                <div>
                   <div class="text-3xl font-black dark:text-white text-gray-900 mb-1">{realisticStats.guidesPublished}</div>
                   <div class="text-[10px] font-black text-gray-400 uppercase tracking-widest">Reports_Filed</div>
                </div>
                <div>
                   <div class="text-3xl font-black dark:text-white text-gray-900 mb-1">{realisticStats.activeReaders}</div>
                   <div class="text-[10px] font-black text-gray-400 uppercase tracking-widest">Active_Nodes</div>
                </div>
              </div>

              <div class="flex gap-6">
                <a href="/blog" class="group relative px-10 py-4 bg-black dark:bg-white text-white dark:text-black font-black uppercase text-sm tracking-widest transition-all text-center rounded-xl shadow-lg italic overflow-hidden">
                  <span class="relative z-10 transition-colors group-hover:text-white dark:group-hover:text-white">Open_Intel_Sync →</span>
                  <div class="absolute inset-0 bg-gradient-to-r from-cyan-500 via-purple-500 to-pink-500 opacity-0 group-hover:opacity-100 transition-opacity"></div>
                </a>
              </div>
           </div>
           
           <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
             {matrixCategories.map((cat, i) => {
               const colors = [
                 'border-cyan-500/20 text-cyan-500', 
                 'border-purple-500/20 text-purple-500', 
                 'border-pink-500/20 text-pink-500'
               ];
               const colorClass = colors[i % colors.length];
               const glowClass = [
                 'group-hover:shadow-cyan-500/20',
                 'group-hover:shadow-purple-500/20',
                 'group-hover:shadow-pink-500/20'
               ][i % 3];
               
               return (
                  <a href={`/categories/${cat}`} class={`p-6 border dark:border-white/10 border-black/5 hover:border-opacity-50 text-[10px] font-black flex justify-between items-center group rounded-2xl bg-white/80 dark:bg-gray-900/80 backdrop-blur-sm transition-all hover:-translate-y-1 ${glowClass} hover:shadow-xl`}>
                    <span class={`font-black uppercase tracking-[0.2em] italic transition-colors group-hover:opacity-100 ${['text-transparent bg-clip-text bg-gradient-to-r from-cyan-500 to-purple-500', 'text-transparent bg-clip-text bg-gradient-to-r from-purple-500 to-pink-500', 'text-transparent bg-clip-text bg-gradient-to-r from-pink-500 to-cyan-500'][i % 3]} dark:opacity-90 opacity-70 group-hover:opacity-100`}>{cat.split('-').join('_').toUpperCase()}</span>
                    <span class={`${colorClass.split(' ')[1]} group-hover:translate-x-1 transition-all`}>→</span>
                 </a>
               )
             })}
           </div>
         </div>
      </div>
    </div>
    
    <style>
      .vertical-text {
        writing-mode: vertical-rl;
        text-orientation: mixed;
      }
    </style>
  </section>

  <!-- 🛠️ PROTOCOL ETHOS (THE MISSION) -->
  <section id="protocol-ethos" class="py-24 dark:bg-black bg-white relative overflow-hidden border-b border-gray-100 dark:border-white/5">
    <!-- Systematic Grid Background -->
    <div class="absolute inset-0 opacity-[0.03] dark:opacity-[0.05]" style="background-image: linear-gradient(#000 1px, transparent 1px), linear-gradient(90deg, #000 1px, transparent 1px); background-size: 40px 40px;"></div>
    
    <div class="container relative z-10 mx-auto px-4">
      <div class="max-w-4xl mx-auto">
        <div class="mb-16 text-center">
            <div class="inline-flex items-center gap-2 px-3 py-1 rounded bg-gray-100 dark:bg-white/5 border border-gray-200 dark:border-white/10 mb-6">
                 <span class="text-[10px] font-black dark:text-gray-400 text-gray-500 uppercase tracking-[0.3em]">Directive_001 // Mission_Statement</span>
            </div>
            <h2 class="text-5xl md:text-7xl font-black dark:text-white text-gray-900 leading-[0.85] uppercase italic tracking-tighter mb-8">
              DeFi Knowledge<br/>
              <span class="text-transparent bg-clip-text bg-gradient-to-r from-cyan-500 via-purple-500 to-pink-500">
                Shouldn't Cost $5,000
              </span>
            </h2>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-12 gap-8 items-stretch">
            <!-- Terminal Message -->
            <div class="lg:col-span-7 dark:bg-white/[0.02] bg-gray-50 border dark:border-white/10 border-gray-200 p-8 md:p-12 rounded-[2rem] relative overflow-hidden group">
                <div class="absolute top-0 right-0 p-6 text-[10px] font-black dark:text-white/10 text-black/10 tracking-widest uppercase vertical-text">INSTITUTIONAL_INTEGRITY</div>
                <div class="flex items-center gap-3 mb-8">
                    <div class="w-2 h-2 rounded-full bg-cyan-500"></div>
                    <span class="text-[10px] font-black dark:text-gray-400 text-gray-500 uppercase tracking-widest">Protocol_Input: Philosophy</span>
                </div>
                <div class="space-y-6">
                    <p class="text-2xl font-black italic dark:text-white text-gray-900 leading-tight">
                        "Crypto was built on transparency. We believe in <span class="text-cyan-500">institutional integrity</span>—sharing the playbook, not just the code."
                    </p>
                    <p class="text-lg dark:text-gray-400 text-gray-600 italic font-medium">
                        We believe when more people understand DeFi, the entire ecosystem grows. <span class="dark:text-white text-black underline">That's our investment.</span>
                    </p>
                </div>
            </div>

            <!-- System Directives List -->
            <div class="lg:col-span-5 flex flex-col gap-4">
                {[
                    { id: 'DIR_01', label: 'OPEN_SOURCE_ETHOS', desc: 'Community-driven research. No courses. No paid shills.' },
                    { id: 'DIR_02', label: 'EQUAL_ACCESS', desc: 'Whether you have $100 or $1M, knowledge is free.' },
                    { id: 'DIR_03', label: 'PUBLIC_BUILD', desc: 'Contributor-powered data shapes the intelligence we ship.' }
                ].map(dir => (
                    <div class="border dark:border-white/5 border-black/5 p-6 rounded-2xl hover:bg-gray-50 dark:hover:bg-white/[0.03] transition-all group">
                        <div class="flex justify-between items-center mb-2">
                            <span class="text-[8px] font-black text-cyan-500 font-mono tracking-widest">{dir.id}</span>
                            <div class="w-1 h-1 rounded-full bg-cyan-500 opacity-0 group-hover:opacity-100 transition-opacity"></div>
                        </div>
                        <h4 class="text-sm font-black dark:text-white text-gray-900 mb-1 uppercase tracking-wider italic">{dir.label}</h4>
                        <p class="text-[10px] dark:text-gray-500 text-gray-400 font-medium italic">{dir.desc}</p>
                    </div>
                ))}
            </div>
        </div>
      </div>
    </div>
  </section>

  <!-- 🎯 LATEST INTEL REPORT (FEATURED) -->
  <section class="py-24 dark:bg-black bg-white relative overflow-hidden border-b border-gray-100 dark:border-white/5">
    <div class="container mx-auto px-4">
      <div class="flex items-center gap-4 mb-12 justify-center">
        <div class="h-px bg-gradient-to-r from-transparent via-cyan-500/50 to-transparent flex-1 opacity-20"></div>
        <div class="text-center px-6">
          <div class="inline-flex items-center gap-2 mb-2 px-3 py-1 rounded-full bg-cyan-500/10 border border-cyan-500/20">
             <span class="text-[8px] font-black text-cyan-500 uppercase tracking-widest italic tracking-[0.4em]">High_Priority_Access</span>
          </div>
          <h2 class="text-3xl lg:text-5xl font-black dark:text-white text-gray-900 uppercase italic tracking-tighter">
            Latest_Intel_<span class="text-transparent bg-clip-text bg-gradient-to-r from-cyan-500 via-purple-500 to-pink-500">Report</span>
          </h2>
        </div>
        <div class="h-px bg-gradient-to-r from-transparent via-cyan-500/50 to-transparent flex-1 opacity-20"></div>
      </div>
      
      <div class="max-w-6xl mx-auto">
        <div class="dark:bg-black bg-white border dark:border-white/10 border-gray-200 rounded-[3rem] overflow-hidden shadow-2xl relative group">
          <!-- Folder Tab Effect -->
          <div class="absolute top-0 right-12 px-6 py-2 bg-gradient-to-r from-cyan-500 to-purple-500 text-white text-[10px] font-black uppercase tracking-widest rounded-b-xl z-20 shadow-lg">FILE_ID: {featuredPost?.slug?.toUpperCase() || "REPORT_001"}</div>
          
          <!-- Visual Container -->
          <div class="lg:col-span-7 relative group">
            <div class="absolute -inset-4 bg-gradient-to-r from-cyan-500/20 via-purple-500/20 to-pink-500/20 rounded-[3rem] blur-2xl opacity-0 group-hover:opacity-100 transition-opacity duration-700"></div>
            <div class="relative overflow-hidden rounded-[2.5rem] border dark:border-white/10 border-gray-200 shadow-2xl">
              <img 
                src={featuredPost.data.image || "/images/placeholder.jpg"} 
                alt={featuredPost.data.title}
                class="w-full aspect-[16/10] object-cover transition-transform duration-700 group-hover:scale-105"
              />
              <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/20 to-transparent"></div>
              
              <!-- Floating Metadata Info -->
              <div class="absolute top-6 left-6 flex gap-3">
                <div class="px-3 py-1 bg-black/60 backdrop-blur-md border border-white/20 rounded-lg text-[10px] font-black text-white uppercase tracking-widest">
                    AES-256-INTEL // ACTIVE
                </div>
                <div class="px-3 py-1 bg-cyan-500 rounded-lg text-[10px] font-black text-black uppercase tracking-widest">
                    AUTH: TXC
                </div>
              </div>

              <!-- Content Overlay -->
              <div class="absolute bottom-10 left-10 right-10">
                <div class="flex items-center gap-4 mb-4">
                  <div class="flex flex-col">
                    <span class="text-[8px] font-black text-cyan-400 uppercase tracking-widest mb-1">READ_TIME</span>
                    <span class="text-xs font-black text-white uppercase tracking-widest">{featuredPost.data.readTime || 4} MIN_SYNC</span>
                  </div>
                  <div class="w-px h-8 bg-white/20"></div>
                  <div class="flex flex-col">
                    <span class="text-[8px] font-black text-green-400 uppercase tracking-widest mb-1">Access_Status</span>
                    <span class="text-xs font-black text-white uppercase tracking-widest">Unrestricted</span>
                  </div>
                </div>
              </div>
            </div>
          </div>

          <!-- Content Details -->
          <div class="lg:col-span-5 flex flex-col justify-center">
            <div class="mb-8 p-4 dark:bg-white/[0.03] bg-gray-50 border dark:border-white/10 border-gray-200 rounded-2xl">
              <p class="text-[10px] font-bold dark:text-gray-500 text-gray-400 uppercase tracking-[0.2em] leading-relaxed">
                #Alpha & Insider Strategies#Early DEX Arbitrage#Cross-Chain
              </p>
            </div>
            
            <h2 class="text-4xl md:text-5xl font-black dark:text-white text-gray-900 mb-6 italic tracking-tighter leading-none uppercase">
                {featuredPost.data.title}
            </h2>
            
            <p class="text-lg dark:text-gray-400 text-gray-600 mb-10 leading-relaxed italic font-medium">
                {featuredPost.data.description || "Discover how to coordinate trades across multiple blockchains, spot inefficiencies, and capture alpha opportunities before public traders react."}
            </p>

            <div class="flex flex-col gap-6">
                <a href={`/blog/${featuredPost.slug}`} class="group relative w-full py-5 bg-black dark:bg-white text-white dark:text-black font-black uppercase text-sm tracking-[0.2em] italic rounded-2xl overflow-hidden shadow-2xl transition-all hover:-translate-y-1 text-center">
                    <span class="relative z-10 group-hover:text-white dark:group-hover:text-white transition-colors">Begin_Synchronization →</span>
                    <div class="absolute inset-0 bg-gradient-to-r from-cyan-500 via-purple-500 to-pink-500 opacity-0 group-hover:opacity-100 transition-opacity"></div>
                        <span class="text-[10px] font-black text-gray-400 dark:text-gray-500">FORMAT: PDF/MD</span>
                        <span class="text-[10px] font-black text-green-500">// 100% FREE</span>
                    </div>
                    <span class="text-[10px] font-black text-gray-400 dark:text-gray-500">AUTH: TXCHYON_AI</span>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>




  <!-- 🔒 RESTRICTED ACCESS MODULES (BONUS PILLARS) -->
  <section id="restricted-modules" class="py-24 dark:bg-black bg-white relative overflow-hidden">
    <!-- Systematic Background -->
    <div class="absolute inset-0 opacity-[0.02] dark:opacity-[0.05]" style="background-image: radial-gradient(#000 1px, transparent 1px); background-size: 20px 20px;"></div>
    
    <div class="container mx-auto px-4 relative z-10">
      <div class="text-center mb-20">
        <div class="inline-flex items-center gap-3 mb-6 px-4 py-2 bg-pink-500/10 border border-pink-500/20 rounded-full">
          <span class="text-pink-500 text-[10px] font-black tracking-[0.4em] uppercase italic">System_Clearance: Level_04_Required</span>
        </div>
        <h2 class="text-5xl md:text-7xl font-black dark:text-white text-gray-900 leading-none uppercase italic tracking-tighter mb-6">
          Restricted_Access_<span class="text-transparent bg-clip-text bg-gradient-to-r from-purple-500 to-pink-500">Modules</span>
        </h2>
        <p class="text-lg dark:text-gray-400 text-gray-600 max-w-2xl mx-auto italic font-medium">Premium intelligence nodes for verified institutional members.</p>
      </div>
      
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-10 max-w-6xl mx-auto">
        <!-- Module 01: Alpha -->
        <div class="relative group">
          <div class="absolute inset-0 bg-gradient-to-br from-purple-500/10 to-pink-500/10 rounded-[2.5rem] blur-2xl group-hover:blur-3xl transition-all duration-500"></div>
          <div class="relative dark:bg-[#080808] bg-gray-50 border dark:border-white/10 border-gray-200 rounded-[2.5rem] p-10 hover:shadow-2xl transition-all duration-500 hover:-translate-y-2">
            <!-- Header Metadata -->
            <div class="flex justify-between items-start mb-10">
              <div>
                <div class="flex items-center gap-3 mb-3">
                  <div class="w-2 h-2 rounded-full bg-purple-500 animate-pulse"></div>
                  <span class="text-[10px] font-black text-purple-500 tracking-widest uppercase italic">Node_ID: ALPHA_INSIDER</span>
                </div>
                <h3 class="text-3xl font-black dark:text-white text-gray-900 leading-tight uppercase italic tracking-tighter">Alpha & Insider<br/>Strategies_</h3>
              </div>
              <div class="px-4 py-2 bg-gradient-to-r from-purple-600 to-pink-600 rounded-lg text-white font-black text-[10px] uppercase tracking-widest shadow-lg">MEMBER_CORE</div>
            </div>

            <!-- Content Grid -->
            <div class="grid grid-cols-2 gap-4 mb-10">
                {[
                  { label: 'Arbitrage_Nodes', icon: '⚡' },
                  { label: 'Whitelist_Sync', icon: '🎯' },
                  { label: 'Alpha_Discovery', icon: '🧠' },
                  { label: 'Insider_Signals', icon: '🔍' }
                ].map(item => (
                  <div class="flex items-center gap-3 dark:bg-white/[0.05] bg-white p-4 rounded-xl border dark:border-white/10 border-black/5">
                    <span class="text-lg">{item.icon}</span>
                    <span class="text-[10px] font-black dark:text-gray-200 text-gray-600 uppercase tracking-wider">{item.label}</span>
                  </div>
                ))}
            </div>

            <div class="space-y-6 pt-6 border-t dark:border-white/5 border-black/5">
                <div class="flex justify-between items-center text-[10px] font-black uppercase tracking-widest text-gray-400">
                    <span>Access_Path: SECURE_VAULT</span>
                    <span class="text-purple-500">ENCRYPTED</span>
                </div>
                <button onclick="document.getElementById('subscribe-popup').showModal()" class="w-full py-5 bg-black dark:bg-white text-white dark:text-black font-black italic uppercase text-sm tracking-[0.2em] rounded-2xl hover:bg-purple-500 dark:hover:bg-purple-500 hover:text-white transition-all shadow-xl group/btn overflow-hidden relative">
                    <span class="relative z-10">Unlock_Access_Module →</span>
                    <div class="absolute inset-0 bg-gradient-to-r from-purple-500 to-pink-500 opacity-0 group-hover/btn:opacity-100 transition-opacity"></div>
                </button>
            </div>
          </div>
        </div>

        <!-- Module 02: Gaming -->
        <div class="relative group">
          <div class="absolute inset-0 bg-gradient-to-br from-cyan-500/10 to-blue-500/10 rounded-[2.5rem] blur-2xl group-hover:blur-3xl transition-all duration-500"></div>
          <div class="relative dark:bg-[#080808] bg-gray-50 border dark:border-white/10 border-gray-200 rounded-[2.5rem] p-10 hover:shadow-2xl transition-all duration-500 hover:-translate-y-2">
            <!-- Header Metadata -->
            <div class="flex justify-between items-start mb-10">
              <div>
                <div class="flex items-center gap-3 mb-3">
                  <div class="w-2 h-2 rounded-full bg-cyan-500 animate-pulse"></div>
                  <span class="text-[10px] font-black text-cyan-500 tracking-widest uppercase italic">Node_ID: GAMING_META</span>
                </div>
                <h3 class="text-3xl font-black dark:text-white text-gray-900 leading-tight uppercase italic tracking-tighter">Web3 Gaming &<br/>Metaverse_</h3>
              </div>
              <div class="px-4 py-2 bg-gradient-to-r from-cyan-600 to-blue-600 rounded-lg text-white font-black text-[10px] uppercase tracking-widest shadow-lg">MEMBER_CORE</div>
            </div>

            <!-- Content Grid -->
            <div class="grid grid-cols-2 gap-4 mb-10">
                {[
                  { label: 'P2E_Economy', icon: '🎮' },
                  { label: 'Land_Equity', icon: '🌐' },
                  { label: 'Asset_Insiders', icon: '🏆' },
                  { label: 'Cross_Game_Ops', icon: '🔄' }
                ].map(item => (
                  <div class="flex items-center gap-3 dark:bg-white/[0.03] bg-white p-4 rounded-xl border dark:border-white/5 border-black/5">
                    <span class="text-lg">{item.icon}</span>
                    <span class="text-[10px] font-black dark:text-gray-400 text-gray-600 uppercase tracking-wider">{item.label}</span>
                  </div>
                ))}
            </div>

            <div class="space-y-6 pt-6 border-t dark:border-white/5 border-black/5">
                <div class="flex justify-between items-center text-[10px] font-black uppercase tracking-widest text-gray-400">
                    <span>Access_Path: GAMING_HUB</span>
                    <span class="text-cyan-500">ENCRYPTED</span>
                </div>
                <button onclick="document.getElementById('subscribe-popup').showModal()" class="w-full py-5 bg-black dark:bg-white text-white dark:text-black font-black italic uppercase text-sm tracking-[0.2em] rounded-2xl hover:bg-cyan-500 dark:hover:bg-cyan-500 hover:text-white transition-all shadow-xl group/btn overflow-hidden relative">
                    <span class="relative z-10">Unlock_Access_Module →</span>
                    <div class="absolute inset-0 bg-gradient-to-r from-cyan-500 to-blue-500 opacity-0 group-hover/btn:opacity-100 transition-opacity"></div>
                </button>
            </div>
          </div>
        </div>
      </div>
      
      <!-- OS Justification Row -->
      <div class="mt-20 max-w-4xl mx-auto">
        <div class="dark:bg-white/[0.02] bg-gray-50 border dark:border-white/10 border-gray-200 p-8 md:p-12 rounded-[2rem] relative overflow-hidden group">
            <div class="grid grid-cols-1 md:grid-cols-3 gap-12">
                {[
                    { id: 'SYS_01', label: 'TIME_SENSITIVE', desc: 'Strategies that lose edge if shared widely.', icon: '⚡' },
                    { id: 'SYS_02', label: 'INSIDER_INTELLIGENCE', desc: 'Direct protocol alpha & private audits.', icon: '🤫' },
                    { id: 'SYS_03', label: 'CLEARANCE_REQUIRED', desc: 'Verified membership for restricted nodes.', icon: '🎯' }
                ].map(item => (
                    <div class="flex flex-col items-center text-center group/item">
                        <div class="w-12 h-12 rounded-2xl bg-gray-100 dark:bg-white/5 flex items-center justify-center mb-6 group-hover/item:bg-gradient-to-br group-hover/item:from-cyan-500/20 group-hover/item:to-purple-500/20 transition-all shadow-sm">
                            <span class="text-2xl">{item.icon}</span>
                        </div>
                        <span class="text-[8px] font-black text-cyan-500 font-mono tracking-widest mb-3">{item.id}</span>
                        <h4 class="text-xs font-black dark:text-white text-gray-900 mb-2 uppercase italic tracking-wider">{item.label}</h4>
                        <p class="text-[10px] dark:text-gray-500 text-gray-400 font-medium italic leading-relaxed">{item.desc}</p>
                    </div>
                ))}
            </div>
            <div class="mt-12 text-center pt-8 border-t dark:border-white/5 border-black/5">
                <p class="text-[9px] dark:text-gray-500 text-gray-400 font-black uppercase tracking-[0.6em] italic animate-pulse">
                    The_11_Foundational_Pillars_remain_100%_Free_Information_Sync_Active
                </p>
            </div>
        </div>
      </div>
    </div>
  </section>

  <!-- 📚 LATEST INTEL FEED (LATEST GUIDES) -->
  <section class="py-24 dark:bg-black bg-white relative overflow-hidden border-b border-gray-100 dark:border-white/5">
    <div class="container mx-auto px-4">
      <div class="flex flex-col lg:flex-row justify-between items-start lg:items-center mb-16 gap-6">
        <div>
          <div class="inline-flex items-center gap-2 mb-3">
            <div class="w-1.5 h-1.5 rounded-full bg-cyan-500"></div>
            <span class="text-[10px] font-black text-gray-400 uppercase tracking-[0.4em]">Node: Intel_Discovery_Feed</span>
          </div>
          <h2 class="text-4xl lg:text-5xl font-black dark:text-white text-gray-900 uppercase italic tracking-tighter">Latest_Research_<span class="text-transparent bg-clip-text bg-gradient-to-r from-cyan-500 to-purple-500">Sync</span></h2>
          <p class="dark:text-gray-500 text-gray-400 mt-2 font-medium italic">Fresh tactical briefings published in real-time.</p>
        </div>
        <a href="/blog" class="px-8 py-3 bg-gray-100 dark:bg-white/5 dark:hover:bg-white/10 border border-gray-200 dark:border-white/10 text-gray-900 dark:text-white text-[10px] font-black uppercase tracking-widest rounded-xl transition-all hover:-translate-y-0.5 shadow-sm">
          <span>📚</span> Open_Archives_
        </a>
      </div>
      {latestPosts && latestPosts.length > 0 ? (
        <>
          <Posts posts={latestPosts} className="mb-16" />
          <div class="flex justify-center">
            <Pagination currentPage={1} totalPages={Math.ceil(allPosts.length / 6)} />
          </div>
        </>
      ) : (
        <div class="text-center py-20 border border-dashed border-gray-200 dark:border-white/10 rounded-[2rem]">
          <div class="text-5xl mb-6 opacity-20 text-center">📖</div>
          <h3 class="text-2xl font-black dark:text-gray-700 text-gray-300 uppercase italic tracking-tighter mb-4">No_Intel_Detected</h3>
          <p class="dark:text-gray-600 text-gray-400 font-medium italic">Scanning for new tactical briefings...</p>
        </div>
      )}
    </div>
  </section>

  <!-- 🎁 FINAL SYSTEM_CLEARANCE (CTA) -->
  <section class="py-24 dark:bg-black bg-white relative overflow-hidden">
    <!-- Final Glow -->
    <div class="absolute bottom-0 left-1/2 -translate-x-1/2 w-[800px] h-[400px] bg-cyan-500/10 rounded-full blur-[120px] pointer-events-none"></div>
    
    <div class="container mx-auto px-4 relative z-10">
      <div class="max-w-4xl mx-auto">
        <div class="dark:bg-[#050505] bg-gray-50 border dark:border-white/10 border-gray-200 rounded-[3rem] p-10 lg:p-16 text-center relative overflow-hidden shadow-2xl group">
          <!-- Terminal Frame Accents -->
          <div class="absolute top-0 left-0 w-24 h-24 border-t-2 border-l-2 border-cyan-500/30 rounded-tl-[3rem]"></div>
          <div class="absolute bottom-0 right-0 w-24 h-24 border-b-2 border-r-2 border-purple-500/30 rounded-br-[3rem]"></div>
          
          <div class="relative z-10">
            <div class="inline-flex items-center gap-3 mb-8">
                <div class="w-12 h-12 rounded-2xl bg-white dark:bg-white/5 flex items-center justify-center shadow-lg border border-gray-100 dark:border-white/10">
                    <span class="text-3xl">🎁</span>
                </div>
            </div>
            
            <h2 class="text-4xl md:text-6xl font-black dark:text-white text-gray-900 mb-8 uppercase italic tracking-tighter leading-none">
                Seriously. It's All_<span class="text-transparent bg-clip-text bg-gradient-to-r from-cyan-500 to-purple-500 animate-pulse">UNRESTRICTED.</span>
            </h2>
            <p class="text-xl dark:text-gray-400 text-gray-600 mb-12 max-w-2xl mx-auto italic font-medium leading-relaxed">
                No credit cards. No trials. No hidden tiers. 
                <span class="text-cyan-500 font-black">Just high-fidelity DeFi intelligence</span> for everyone building in public.
            </p>
            
            <div class="flex flex-col sm:flex-row gap-6 justify-center items-center">
                <a href="/blog" class="group relative px-12 py-5 bg-black dark:bg-white text-white dark:text-black font-black uppercase text-sm tracking-[0.2em] italic rounded-2xl overflow-hidden shadow-2xl transition-all hover:-translate-y-1">
                    <span class="relative z-10 group-hover:text-white dark:group-hover:text-white transition-colors">Invoke_Free_Archives ⚡</span>
                    <div class="absolute inset-0 bg-gradient-to-r from-cyan-500 via-purple-500 to-pink-500 opacity-0 group-hover:opacity-100 transition-opacity"></div>
                </a>
                <div class="flex flex-col items-start text-left">
                    <p class="text-[10px] text-gray-500 dark:text-gray-400 font-bold uppercase tracking-widest mb-1 flex items-center gap-2">
                        <span class="w-1.5 h-1.5 rounded-full bg-green-500"></span>
                        {realisticStats.activeReaders} DATA_POINTS_SYNCED
                    </p>
                    <p class="text-[8px] text-gray-400 dark:text-gray-500 font-mono tracking-widest">v3.1_STABLE_REL</p>
                </div>
            </div>

            <div class="mt-16 pt-8 border-t dark:border-white/5 border-black/5 opacity-50">
                <p class="text-[8px] dark:text-gray-600 text-gray-400 font-mono uppercase tracking-[0.4em] leading-relaxed">
                    Disclaimer: Not_Financial_Advice // DeFi_Involves_Risk // DYOR // Protocol_Verification: TXCHYON_MAINNET
                </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- NEWSLETTER POPUP -->
  <dialog id="subscribe-popup" style="position: fixed; top: 50%; left: 50%; transform: translate(-50%, -50%); padding: 20px; border: none; border-radius: 12px; box-shadow: 0 20px 60px rgba(0,0,0,0.3); max-width: 500px; width: 90%; background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%); color: white; z-index: 9999; margin: 0;">
    <div style="text-align: center; padding: 20px;">
      <div style="font-size: 48px; margin-bottom: 16px;">🚀</div>
      <h2 style="font-size: 24px; font-weight: bold; margin-bottom: 12px; color: #60a5fa; text-align: center;">Stay Ahead in Crypto</h2>
      <p style="color: #cbd5e1; margin-bottom: 20px; line-height: 1.5; text-align: center;">Get exclusive DeFi insights, airdrop alerts & alpha directly to your inbox.<br><span style="color: #10b981; font-weight: 600;">No spam, just pure value.</span></p>
      <script async src="https://subscribe-forms.beehiiv.com/embed.js"></script>
      <iframe src="https://subscribe-forms.beehiiv.com/62d85daa-7878-4a83-a78a-a15f6226c516" class="beehiiv-embed" data-test-id="beehiiv-embed" frameborder="0" scrolling="no" style="width: 508px; height: 198px; margin: 0; border-radius: 0px !important; background-color: transparent; box-shadow: 0 0 #0000; max-width: 100%;"></iframe>
      <button id="close-popup" style="margin-top: 20px; padding: 10px 20px; background: #374151; color: #d1d5db; border: none; border-radius: 8px; cursor: pointer; font-weight: 500; transition: all 0.2s ease;">No thanks, I'll miss out on alpha</button>
    </div>
  </dialog>

  <script>
    document.addEventListener('DOMContentLoaded', () => {
      const popup = document.getElementById('subscribe-popup');
      const closeButton = document.getElementById('close-popup');
      let popupShown = false;
      function showPopup() {
        if (!popupShown && !localStorage.getItem('popupShownToday')) {
          popup.showModal();
          popupShown = true;
          localStorage.setItem('popupShownToday', new Date().toISOString());
        }
      }
      setTimeout(showPopup, 30000);
      closeButton.addEventListener('click', () => popup.close());
    });
  </script>

  <style>
    /* ===== GLOBAL DARK MODE CARD FIX =====
       Any element with bg-white inside .dark gets overridden to dark gray.
       This catches all post cards, feature boxes, and any missed containers. */
    .dark .bg-white {
      background-color: rgb(17 17 17) !important;
    }
    .dark .bg-white\/60,
    .dark .bg-white\/80,
    .dark .bg-white\/40 {
      background-color: rgba(17, 17, 17, 0.9) !important;
    }
    /* Ensure text inside dark cards is readable */
    .dark .bg-white p,
    .dark .bg-white h1,
    .dark .bg-white h2,
    .dark .bg-white h3,
    .dark .bg-white h4,
    .dark .bg-white span:not([class*='text-']) {
      color: rgb(229 231 235);
    }
    /* ===================================== */
    @keyframes gradient { 0%, 100% { background-position: 0% 50%; } 50% { background-position: 100% 50%; } }
    .animate-gradient { background-size: 200% 200%; animation: gradient 4s ease infinite; }
    @keyframes pulse { 0%, 100% { opacity: 1; } 50% { opacity: 0.5; } }
    .animate-pulse { animation: pulse 3s ease-in-out infinite; }
    .stars { position: absolute; top: 0; left: 0; width: 100%; height: 100%; background-image: radial-gradient(2px 2px at 20px 30px, #eee, transparent), radial-gradient(2px 2px at 40px 70px, #fff, transparent), radial-gradient(1px 1px at 90px 40px, #ddd, transparent), radial-gradient(1px 1px at 130px 80px, #fff, transparent), radial-gradient(2px 2px at 160px 20px, #ddd, transparent), radial-gradient(1px 1px at 200px 120px, #fff, transparent), radial-gradient(2px 2px at 250px 60px, #eee, transparent), radial-gradient(1px 1px at 300px 100px, #ddd, transparent); background-repeat: repeat; background-size: 800px 800px; animation: twinkle-slow 8s infinite; }
    .stars-2 { animation-delay: 2s; opacity: 0.6; background-size: 900px 900px; animation-duration: 10s; }
    .stars-3 { animation-delay: 4s; opacity: 0.4; background-size: 1000px 1000px; animation-duration: 12s; }
    .shooting-star { position: absolute; top: 0; left: -100px; width: 120px; height: 1px; background: linear-gradient(90deg, transparent, rgba(96, 165, 250, 0.4), transparent); animation: shooting-star-slow 30s linear infinite; }
    .shooting-star-2 { top: 25%; animation-delay: 10s; }
    .shooting-star-3 { top: 65%; animation-delay: 20s; }
    .animate-pulse-slow { animation: pulse-slow 6s ease-in-out infinite; }
    @keyframes twinkle-slow { 0%, 100% { opacity: 0.6; } 50% { opacity: 0.9; } }
    @keyframes shooting-star-slow { 0% { transform: translateX(0) translateY(0) rotate(30deg); opacity: 0; } 5% { opacity: 0.6; } 15% { transform: translateX(100vw) translateY(80px) rotate(30deg); opacity: 0; } 100% { opacity: 0; } }
    @keyframes pulse-slow { 0%, 100% { opacity: 0.03; } 50% { opacity: 0.08; } }
    @keyframes pulse-slower { 0%, 100% { opacity: 0.02; } 50% { opacity: 0.05; } }
    .animate-pulse-slower { animation: pulse-slower 8s ease-in-out infinite; }
    .particle-field { position: absolute; inset: 0; overflow: hidden; }
    .particle { position: absolute; border-radius: 50%; animation: particle-float-slow linear infinite; }
    .particle-fast { width: 2px; height: 2px; background: rgba(139, 92, 246, 0.5); animation-duration: 8s; }
    .particle-slow { width: 1px; height: 1px; background: rgba(59, 130, 246, 0.3); animation-duration: 20s; }
    @keyframes particle-float-slow { 0% { transform: translateX(0) translateY(0); opacity: 0; } 10% { opacity: 0.6; } 100% { transform: translateX(100vw) translateY(80vh); opacity: 0; } }
    * { transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1); }
    .line-clamp-1 { overflow: hidden; display: -webkit-box; -webkit-box-orient: vertical; -webkit-line-clamp: 1; }
  </style>
</Base>